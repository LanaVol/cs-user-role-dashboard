import React from "react";

/**
 * StaffList component representing the staff list page of the application.
 * @component
 * @returns {JSX.Element} - The JSX for the StaffList component.
 */
function StaffList() {
  return (
    <div className="w-full h-[90%] rounded-b-lg bg-dark border-2 border-light8 border-t-transparent p-10 pt-[27px]">
      <div className="p-4 bg-primary rounded-lg text-grey">
        Список персонала
      </div>
    </div>
  );
}

export default StaffList;
